import { useEffect, useState } from "react";


const useTools = (props,toolId) => {
   
    // const [tools, setTools] = useState({});

    // useEffect(() => {
    //     const url = `https://ancient-inlet-12712.herokuapp.com/tools/${tools}`;
    //     console.log(url);
    //     fetch(url)
    //         .then(res => res.json())
    //         .then(data => setTools(data));

    // }, []);
    // return [tools]
}

export default useTools;